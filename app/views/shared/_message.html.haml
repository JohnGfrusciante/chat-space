.mainChat__contents__post{"data-messege_id": message.id}
  .mainChat__contents__post__userName
    = message.user.name
    .mainChat__contents__post__userName__timeStamp
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .mainChat__contents__post__message
    - if message.content.present?
      %p
        = message.content
    = image_tag message.image.url, class: '' if message.image.present?